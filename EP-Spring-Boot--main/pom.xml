<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.4.13</version>
		<relativePath /> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.jspider</groupId>
	<artifactId>spring-boot-simple-crud-with-mysql</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>spring-boot-simple-crud-with-mysql</name>
	<description>Demo project for Spring Boot</description>
	<url />
	<licenses>
		<license />
	</licenses>
	<developers>
		<developer />
	</developers>
	<scm>
		<connection />
		<developerConnection />
		<tag />
		<url />
	</scm>
	<properties>
		<java.version>17</java.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<!-- Phase 3 (AAP §0.6.1) — Spring Security 6.4.x (parent-managed). Triggers
		     WebSecurityAutoConfiguration; enables HeadersConfigurer, CorsConfigurationSource,
		     authorizeHttpRequests, and @PreAuthorize. Closes findings #1, #4, #8, #9, #10.
		     This is the helmet.js → Spring Security translation defined in AAP §0.1.2/§0.1.3. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<!-- Phase 4 (AAP §0.6.1) — Jakarta Bean Validation 3.x via Hibernate Validator 8.x
		     (parent-managed). Provides jakarta.validation.constraints.* (@NotBlank, @Size,
		     @NotNull, @Positive, @DecimalMax) and triggers MethodArgumentNotValidException
		     from @Valid controller parameters. Closes finding #2. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>com.mysql</groupId>
			<artifactId>mysql-connector-j</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Phase 6 (AAP §0.6.1) — Spring Security test helpers (parent-managed, test scope).
		     Provides @WithMockUser, SecurityMockMvcRequestPostProcessors.csrf(),
		     httpBasic(...) request post-processors required by SecurityConfigTest and
		     ProductControllerSecurityTest. -->
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<!-- https://mvnrepository.com/artifact/org.springdoc/springdoc-openapi-starter-webmvc-ui -->
		<!-- Phase 2 (AAP §0.6.1) — version bumped from 2.8.6 to latest 2.8.x patch (2.8.17)
		     within the 2.x major line for cumulative springdoc + Swagger UI fixes.
		     Springdoc is NOT in the Spring Boot BOM so the explicit <version> pin remains. -->
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>2.8.17</version>
		</dependency>
		<!-- Phase 5 (AAP §0.6.1) — Bucket4j 8.10.1 token-bucket rate limiter.
		     Explicit <version> required because Bucket4j is NOT in the Spring Boot BOM.
		     Provides io.github.bucket4j.{Bucket,Bandwidth,Refill} used by RateLimitingFilter.
		     Closes finding #4 (no rate limiting). -->
		<dependency>
			<groupId>com.bucket4j</groupId>
			<artifactId>bucket4j-core</artifactId>
			<version>8.10.1</version>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
